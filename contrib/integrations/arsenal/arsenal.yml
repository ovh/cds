name: Arsenal
default_config:
  host:
    type: string
  gw.source:
    type: string
  gw.token:
    type: password
  gw.service:
    type: string
deployment: true
additional_default_config:
  version:
    type: string
    value: "{{.cds.version}}"
  alternative.config:
    type: text
    value: ""
  deployment.token:
    type: password
  retry.max:
    type: string
    value: 10
  retry.delay:
    type: string
    value: 5
