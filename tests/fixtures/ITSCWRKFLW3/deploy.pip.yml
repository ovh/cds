version: v1.0
name: deploy
stages:
- Deploy

jobs:

- job: Deploy
  stage: Deploy
  description: Deploy with an integration
  steps:
  - script:
    - '#echo ''Deploy in progress...'' '
    - sleep 1
    - '#echo ''Deployed'' '
