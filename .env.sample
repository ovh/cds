# CDS Environment Configuration
# Copy this file to .env and customize with secure values

###############################################################################
# SECURITY WARNING: 
# Change these default values before deploying to production!
# Use strong, randomly-generated passwords.
###############################################################################

# PostgreSQL Configuration
POSTGRES_USER=cds
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=cds

# Redis Configuration
REDIS_PASSWORD=CHANGE_ME_STRONG_REDIS_PASSWORD

# Optional: Override CDS Docker Image
# CDS_DOCKER_IMAGE=ovhcom/cds-engine:latest

###############################################################################
# Quick Start Guide:
# 
# 1. Copy this file: 
#    cp .env.sample .env
#
# 2. Generate strong passwords (Linux/macOS):
#    openssl rand -base64 32
#
# 3. Edit .env with your secure passwords:
#    nano .env
#    
# 4. Verify configuration:
#    docker-compose config | grep -E "POSTGRES_PASSWORD|REDIS_PASSWORD"
#
# 5. Start CDS:
#    docker-compose up -d
#
###############################################################################
